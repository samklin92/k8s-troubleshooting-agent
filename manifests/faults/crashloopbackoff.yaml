apiVersion: v1
kind: Pod
metadata:
  name: broken-crash-pod
  namespace: default
  labels:
    fault-type: crash-loop-backoff
spec:
  containers:
    - name: app
      image: busybox:1.36
      command: ["sh", "-c", "echo 'Fatal error: missing required config file /etc/app/config.yaml' && exit 1"]
